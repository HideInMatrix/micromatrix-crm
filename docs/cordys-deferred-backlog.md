# Cordys 暂缓能力与数据模型缺口台账

本台账记录源码对齐过程中已经确认、但当前阶段尚未实施的能力。目的不是描述模糊的“以后再做”，而是保证整体复刻结束时每个已发现缺口都有明确去向。

最近复查：2026-08-30（W3.7-9.2 DB-010 通用审批资源快照已完成并 `VERIFIED`）。当前核心回归基线：隔离空库 **59/59 migrations + 双次 Seed**、DB-010 generic lifecycle + Quote/Invoice/Order approval regression 全绿、W3.6.3 Contract HTTP Smoke 在 59 migrations 下全绿、DB-021 runtime Smoke **12/12**、DB-021 PC/Mobile Browser **25/25**、W3.6.6 `/system/modules` Browser **47/47**、合同卡 Browser **23/23**、根级 Smoke **227/227**、rules **119/119**；API typecheck、workspace ESLint、Shared/API/Web production build 全绿。本地 migration 总数为 **59**。DataEase provider/token 继续由 DB-023 deferred。

## 管理规则

- 新发现的 Cordys 能力只要当前不实施，就必须先登记再结束当前阶段。
- 数据库缺少模型、字段、枚举、关系或审计信息时，必须在“数据模型缺口”列写明。
- 状态只允许 `DISCOVERED`、`PLANNED`、`IN_PROGRESS`、`VERIFIED`、`NOT_APPLICABLE`；不得通过删除条目表示完成。
- 只有源码、迁移、API、页面、权限和测试均完成时才能标记 `VERIFIED`。
- `cordys-parity.md` 只有在关联条目全部 `VERIFIED` 或有证据标记 `NOT_APPLICABLE` 后，才允许把对应模块标为完整对齐。

## 当前台账

| ID     | 能力/事件                        | Cordys 源码依据                                                                                                                   | 当前实现缺口                                                                                                                                                                                                            | 数据模型缺口                                                                                                                                                                                      | 进入实施的前置条件                                              | 状态          |
| ------ | -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------------- |
| DB-001 | 合同作废 `CONTRACT_VOID`         | `ContractStage.VOID`、`ContractStageRequest.voidReason`、`ContractService.updateStage`                                            | W3.6.3 已完成 direct 合同阶段：默认 `VOID/作废` 阶段、作废原因必填、Snapshot 同步，并在 W3.6.6 7.4 补齐 `BusinessNotificationsService.sendConfigured(CONTRACT_VOID)`；isolated contract HTTP Smoke 已验证 `合同已作废` 通知。 | `contract_stage_config` + `Contract.voidReason/createUser/updateUser/createTime/updateTime` 已覆盖最终 direct 模型；早期台账假设的固定 `ContractStatus`/额外 voidedAt/voidedBy 不再作为最终 Cordys direct DDL。 | W3.6.3 direct runtime + W3.6.6 7.4 通知回归均通过 | `VERIFIED` |
| DB-002 | 合同归档 `CONTRACT_ARCHIVED`     | `ContractStage.ARCHIVED`、`ContractService.updateStage`                                                                           | W3.6.3 默认阶段已包含 `ARCHIVED/合同完结`，使用真实 stage config 流转、Snapshot/pos 审计；W3.6.6 7.4 已接 `CONTRACT_ARCHIVED` 配置消息并由 isolated contract HTTP Smoke 验证 `合同已归档` 通知。 | 最终模型采用 `contract_stage_config.id` + `Contract.stage/updateUser/updateTime`，不再依赖旧固定 `ContractStatus`；动态阶段、NORMAL/ADVANCED、回退均已建模。 | W3.6.3 stage/runtime + W3.6.6 7.4 通知回归均通过 | `VERIFIED` |
| DB-003 | 发票审批 `INVOICE_APPROVAL`      | `ApprovalActionService.sendFinishNotice` 的 `FormKey.INVOICE` 分支、`DictModule.INVOICE_APPROVAL`                                 | W3.6.4 已完成 direct Invoice CREATE/UPDATE/DELETE 审批、Snapshot 回滚、DELETE 延迟执行、统一 `approval-resource` 详情/撤回，并将审批完成业务事件映射为 `INVOICE_APPROVAL`。 | `ContractInvoice.approvalStatus/approved/createUser/updateUser`、Invoice Field/Blob/Snapshot 与统一 ApprovalInstance 已落库，旧 `InvoiceRecord` 假设已淘汰。 | `pnpm smoke:w364-invoice-approval` 本轮再次 PASS；Rules/Browser/空库在 W3.6.4 已验收 | `VERIFIED` |
| DB-004 | 配置范围中的创建人 `CREATE_USER` | `CommonNoticeSendService.getNoticeReceiveUserIds`、Cordys `BaseModel.createUser`                                                  | Quote、Contract、Payment Plan/Record、Invoice、Order 等 direct 创建接口均写入 `createUser`；报价/合同/回款计划到期通知及合同作废/归档配置消息都把真实 `createUserId` 传入收件范围解析。 | 交易链 direct 表已具备 `create_user/update_user/create_time/update_time`；MessageSettings `CREATE_USER` 不再缺业务上下文。 | W3.6.0～W3.6.5 direct runtime + W3.6.6 7.4 源码/通知回归通过 | `VERIFIED` |
| DB-005 | 回款计划独立负责人               | `ContractPaymentPlan.owner`、`NoticeExpireJob.buildPaymentPlanNoticeContext`                                                      | W3.6.4 `ContractPaymentPlan.owner` 已成为独立字段：新增/编辑/批量修改、列表/详情、DataScope 均使用计划负责人；到期通知使用 `plan.owner` 而非合同负责人。 | `contract_payment_plan.owner/create_user/update_user` 已进入 direct 模型，部门展示由负责人实时映射；Field/Blob 同步完成。 | W3.6.4 payment runtime/Browser + W3.6.6 7.2 DataScope 验收通过 | `VERIFIED` |
| DB-006 | 企业微信消息渠道                 | `MessageTask.weComEnable`、消息设置企微列、`WeComNoticeSender`                                                                    | 已完成逐事件/批量开关、渠道 gate、文本应用消息、缺映射审计、有限重试、超时恢复、分页记录和手工重试                                                                                                                      | 已新增 `weComEnabled` 与 `message_deliveries`；保存外部主体快照、状态、尝试次数、下次时间、provider 消息 ID 和安全错误摘要                                                                        | W3.1 配置、W3.2 映射与 W3.3 迁移/API/页面/自动化全部通过        | `VERIFIED`    |
| DB-007 | 公告                             | `views/system/message/index.vue` 公告 Tab 及公告后端模块                                                                          | 当前消息设置只实现消息通知 Tab                                                                                                                                                                                          | 缺少公告、公告接收范围、已读状态及附件关系模型                                                                                                                                                    | 独立读取公告页面、API、Service、Domain、Mapper 后立项           | `DISCOVERED`  |
| DB-008 | 消息模板与多语言资源             | `MessageTemplateUtils`、后端 i18n 资源                                                                                            | 当前通知正文由业务 Service 中文字符串生成，尚未完全复刻 Cordys 模板变量和多语言渲染                                                                                                                                     | 若沿用资源文件无需业务表；如需租户自定义模板，必须先确认 Cordys 是否存在对应能力，禁止臆造模板表                                                                                                  | 完成国际化公共底座源码对齐                                      | `DISCOVERED`  |
| DB-009 | 审批流版本与基础图结构           | `ApprovalFlowVersion`、`ApprovalNode`、`ApprovalNodeApprover`、`ApprovalNodeLink`                                                 | W2.5 已完成主记录、不可变版本、起止/审批节点、线性连接、实例版本引用及旧线性配置迁移                                                                                                                                    | 已补 `currentVersionId/number/formType/execute flags/deleted/audit`、版本、审批节点扩展和连接；条件节点结构归 DB-012                                                                              | 源码、迁移、API、页面、权限、规则测试、Smoke 和浏览器验收均通过 | `VERIFIED`    |
| DB-010 | 编辑审批的通用资源快照与变更上下文 | `ApprovalResourceService`、`ApprovalResourceSnapshot`、`ApprovalActionService.revertFromSnapshot`、`approval_instance.execute_time/update_fields/comment` | W3.7-9.2 已把 Quote/Contract/Invoice/Order 的 capture/restore/status/delete 收口到显式 Resource boundary；UPDATE 实例真实保存 `updateFields/comment`，reject/submitter revoke 从通用快照恢复并清理，UPDATE approve 清理失效快照，DELETE 继续审批通过后才实际删除；四业务专项和跨租户 restore fail-closed 均已验收。 | Migration 58 建立 `approval_resource_snapshots` 并迁入仍在途的旧 UPDATE 快照；Migration 59 删除 `approval_instances.business_snapshot`。当前通用表是唯一活动快照真相源，production `businessSnapshot=0`，`ApprovalsService` 四业务 direct Prisma 引用=0。 | W3.7-9.2A～D、59/59 空库双 Seed、Rules 119/119、Root 227/227、typecheck/lint/build 全部通过 | `VERIFIED` |
| DB-011 | 高级审批任务、记录与附件         | `ApprovalAddSignTask`、`ApprovalReturnBackRecord`、`ApprovalRecord`、`ApprovalInstanceAttachment`、任务 `type/action/nodeRound`   | W3.7-9.3A～C 已完成：task 稳定 nodeId/nodeRound/type/action、不可变 ApprovalRecord、BEFORE/AFTER 嵌套加签链、节点退回、ReturnBackRecord、round 重建及 PC/Mobile 入口均为真实 runtime。当前只剩 9.3D 审批人任务撤回与 9.3E requireComment/附件。 | Migration 60 已建立 ApprovalRecord 与 task round/action；61 已建立 ApprovalAddSignTask；62 已建立 ApprovalReturnBackRecord。仍缺 `ApprovalInstanceAttachment` 及 task-revoke 对应动作记录结构。 | 9.3A～C 已验收；继续 9.3D task revoke，再完成 9.3E comment/attachment 后关闭 DB-011 | `IN_PROGRESS` |
| DB-012 | 高级审批节点配置                 | `ApprovalNodeApprover`、`ApprovalNodeCondition` 及流程设计器配置                                                                  | 基础抄送已在 W3.4.1 落地：节点 `ccUserIds`、`approval_task.taskType=CC`、`my-copied` 查询和首页入口均已闭环；仍缺自动通过/拒绝、连续上级/部门负责人、空审批人兜底、同提交人策略、条件分支、后置动作、字段权限和 Webhook | 已具备基础 cc 用户列表与 CC 任务类型；仍缺动态 approver/fallback/sameSubmitter、条件 JSON、通过/驳回配置、字段权限及 Webhook 配置/执行审计                                                        | 基础 CC 已完成；其余高级节点能力按流程设置后续独立执行单元接入  | `DISCOVERED`  |
| DB-013 | 企微组织同步与资源映射           | `ThirdDepartmentService`、`WeComDepartmentService`、`UserSyncController`、`DataHandleUtils`、组织同步 DTO/Domain/Mapper           | 已完成配置门槛、部门/成员全量快照、差异预览、冲突绑定/跳过、原子应用、幂等、缺失成员禁用、页面、日志和通知                                                                                                              | 已落地部门/成员映射、同步批次/差异、凭据版本、结果与最近同步状态；增量游标、多部门关系、unionId/openUserId、定时同步及其他 provider 另行跟踪                                                      | W3.1 配置底座；W3.2 规格、迁移、API、页面与自动化验收全部完成   | `VERIFIED`    |
| DB-014 | 企微统一登录与外部身份           | `SSOController/Service`、`OAuthStateFlow.QR_WECOM/WECOM`、`sys_user/sys_user_extend` 及组织成员绑定                               | 已完成 PC 官方扫码与工作台 WebView OAuth 双流程、一次性回调、JWT、身份管理、资料补全和分类型登录审计                                                                                                                    | flow 使用 `QR_WECOM/WECOM`；邮箱可空、性别为 Boolean、头像进入独立 `user_extensions`；原始安全材料不落库，全部迁移已从零验证                                                                      | 数据库重建、Seed、生产构建、19 条专项 Smoke 与最终检查均通过    | `VERIFIED`    |
| DB-015 | 钉钉/飞书登录与消息 provider     | `MessageTask.dingTalkEnable/larkEnable`、对应 OAuth、同步与 NoticeSender                                                          | W3.3 仅实现企业微信；钉钉/飞书的配置、成员映射、统一登录和消息 sender 尚未实现                                                                                                                                          | 现有 provider/channel 枚举已预留；仍缺对应安全配置、外部资源映射、登录协议适配与 sender                                                                                                           | 分别读取 Cordys 对应前后端全链路后独立立项                      | `DISCOVERED`  |
| DB-016 | 线索直接模型与线索池分域表       | `Clue`、`ClueOwner`、`ClueField/Blob`、`CluePool`、`CluePoolHiddenField`、Pick/Recycle Rule、`ClueCapacity`                       | 直接模型、分域 Repository、规则、Owner 生命周期、业务调用方、Seed、普通线索/转换/多池 API、分域配置、模块设置 Pool/Capacity/Move Reason 与 PC 页面均已完成并通过 W3.4.2 最终验收                                        | 线索 9 表、字段值事务、池事务、Owner History、Pool/Capacity 配置、Hidden Field、PICK/ASSIGN、自动回收、User View 及 `CLUE_POOL_RS` 原因集成均有真实库/API/Browser 证据                            | W3.4.0 task 1.1～1.9 与 W3.4.2 task 3.1～3.7 全部完成           | `VERIFIED`    |
| DB-017 | 客户/联系人/公海直接模型         | `Customer`、`CustomerOwner`、`CustomerField/Blob`、`CustomerContact*`、`CustomerPool*`、`CustomerCapacity/Relation/Collaboration` | 直接模型、分域 Repository、Seed、普通客户/360、联系人、协作/关系/合并、多客户公海 API/规则以及桌面/Mobile 页面均已完成；W3.4.5 最终客户 Browser 23/23 与全图验收已通过 | 现有直接表覆盖 Cordys 最终 DDL；nullable 联系人、Pool/Capacity/Owner History、动态字段、自动回收与四类访问边界均有真实 API/数据库证据，无新增数据库缺口 | W3.4.0、W3.4.3 与 W3.4.5 全部完成 | `VERIFIED` |
| DB-018 | 仪表板目录、资源与收藏           | `Dashboard`、`DashboardModule`、`DashboardCollection`、`DashboardController/ModuleController`                                     | 三张直接表、Seed、目录/资源 API、权限、Scope、组织隔离、排序、收藏、URL/iframe 安全策略与 `/reports` Vue 页面均已完成；Dashboard Browser 28/28、API 44/44                                                                      | `dashboard`、`dashboard_module`、`dashboard_collection` 已进入迁移并通过 Seed、真实 API、Browser、根级回归；DataEase provider 不属于本项，由 DB-023 单独跟踪                                      | W3.4.0 与 W3.4.4 task 5.1～5.6 已完成                           | `VERIFIED`    |
| DB-019 | 用户视图直接模型                 | `UserView`、`UserViewCondition`、`sys_user_view`、`sys_user_view_condition`                                                       | 五类资源 API、Web 契约和业务列表筛选均已切换到 UserViews Service；系统视图保持代码事实，不入用户视图表；1.9 根回归已再次验证 UserView 新建、筛选、固定与删除                                                            | `sys_user_view/sys_user_view_condition` 直接模型、条件文本/树节点序列化、组织/用户/resourceType 隔离均已通过空库迁移、规则测试和真实库 Smoke                                                      | W3.4.0 task 1.1～1.9 已完成                                     | `VERIFIED`    |
| DB-020 | 模块表单、字段与分域字段值       | `ModuleForm/Blob`、`ModuleField/Blob`、`BaseResourceField`、Clue/Customer/Contact Field 与 Blob 表                                | ModuleForms、Metadata adapter 与线索/客户/联系人三目标业务调用方均已接入直接字段值；CRUD、导入、批改、筛选、唯一规则和客户合并链均已完成最终页面/API 验收 | 模块表单/字段 4 表及三业务域 Field/Blob 已进入迁移，旧 FieldDefinition 已删除；W3.4.5 空库再次验证目标表/索引/Seed，图外模块动态字段继续由 DB-021 跟踪 | W3.4.0、W3.4.2、W3.4.3、W3.4.5 全部完成 | `VERIFIED` |
| DB-021 | 图外业务模块分域动态字段值       | Cordys Product/Opportunity/Quotation/Contract/Order/FollowUpPlan 对应 ModuleForm、Field/Blob Domain 与 Mapper                     | W3.6 已关闭 Opportunity、Product/Price、Quotation、Contract、Payment Plan、Payment Record、Invoice、Order；DB-021 8.1～8.5 又完成 FollowUpPlan ModuleForm/Metadata、独立 Field/Blob、CRUD/list/detail/filter/delete、Lead 转换复制、Customer merge 与 PC/Mobile 动态字段真实往返。FollowUpPlan production runtime `customData=0`，Browser 25/25、runtime Smoke 12/12、空库 57/57 + 双 Seed 全绿。 | Opportunity/Product/Price/Quotation/Contract/PaymentPlan/PaymentRecord/Invoice/Order/FollowUpPlan 分域 Field/Blob 均已存在。`follow_up_plans.customData` 仅保留为无法安全猜测历史 key->fieldId 时的升级兼容物理列，当前 runtime 不读写且不再是真相源；未重新引入 FieldDefinition/JSONB 动态字段实现。 | DB-021 8.1～8.5 源码、migration、API、PC/Mobile、复制/合并链、Browser、Rules、Root/空库与静态检查全部通过 | `VERIFIED` |
| DB-022 | 模块配置专属设置入口             | `views/system/module/components/configCard.vue` 及各模块设置抽屉/页面                                                             | 线索/客户及 W3.6 交易链专属设置均已 REAL：商机表单/报价/阶段/关闭规则/失败原因，产品/价格表，合同/回款计划/回款记录/工商抬头必填/发票/合同阶段，订单表单/状态流均绑定真实 route/drawer，无 label-only 占位。 | 所有当前模块设置入口均消费 direct metadata/config API；W3.6.6 最终 `/system/modules` Browser Smoke **47/47**，合同卡补充回归 **23/23**。 | W3.4 线索/客户 + W3.6.0～W3.6.5 + W3.6.6 7.4 全部通过 | `VERIFIED` |
| DB-023 | DataEase 仪表板 provider 适配    | Cordys `DataEaseService`、`/organization/settings/de-token`、第三方配置、CRM→DE 数据范围变量同步                                   | 当前产品决策为暂不实现；Dashboard 主链只保留通用 HTTPS URL、精确 iframe origin/CSP/postMessage policy，不落 DataEase 配置/token                                                                                           | 未新增 Prisma 模型、migration 或 Secret 存储；后续若恢复需重新确认 DataEase 版本、配置契约、token 协议、Scope 变量和 provider 错误分类                                                         | 独立立项；W3.4 已 VERIFIED，本项不阻塞该结论                    | `DEFERRED`    |
