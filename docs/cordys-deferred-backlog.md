# Cordys 暂缓能力与数据模型缺口台账

本台账记录源码对齐过程中已经确认、但当前阶段尚未实施的能力。目的不是描述模糊的“以后再做”，而是保证整体复刻结束时每个已发现缺口都有明确去向。

最近复查：2026-08-28（W3.4.3 task 4.3 联系人 API 已完成，主线进入 task 4.4）。当前回归基线新增联系人 API Smoke 18/18，并继续保持客户 API/360 Smoke 22/22、114 条规则测试、线索模块设置 API 22/22、模块设置 Browser 17/17、线索连续生命周期 17/17、普通线索 API 18/18、三条转换 21/21、多线索池 32/32、线索 Browser 20/20、W3.4.1 真实库 Smoke 17/17、219 条关键链路及既有企业设置/W3.2/W3.3 回归；本地 migration 总数为 **35**。联系人 `customerId` nullable 已在 API 层落地，本任务无 migration；DB-016 与 DB-019 已为 `VERIFIED`，DB-017、DB-018、DB-020 仍保持 `IN_PROGRESS`，DB-021/DB-022 继续按原边界跟踪。

## 管理规则

- 新发现的 Cordys 能力只要当前不实施，就必须先登记再结束当前阶段。
- 数据库缺少模型、字段、枚举、关系或审计信息时，必须在“数据模型缺口”列写明。
- 状态只允许 `DISCOVERED`、`PLANNED`、`IN_PROGRESS`、`VERIFIED`、`NOT_APPLICABLE`；不得通过删除条目表示完成。
- 只有源码、迁移、API、页面、权限和测试均完成时才能标记 `VERIFIED`。
- `cordys-parity.md` 只有在关联条目全部 `VERIFIED` 或有证据标记 `NOT_APPLICABLE` 后，才允许把对应模块标为完整对齐。

## 当前台账

| ID     | 能力/事件                        | Cordys 源码依据                                                                                                                   | 当前实现缺口                                                                                                                                                                                                            | 数据模型缺口                                                                                                                                                                                      | 进入实施的前置条件                                              | 状态          |
| ------ | -------------------------------- | --------------------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------- | ------------- |
| DB-001 | 合同作废 `CONTRACT_VOID`         | `ContractStage.VOID`、`ContractStageRequest.voidReason`、`ContractService.updateStage`                                            | 当前合同只有 `TERMINATED`，没有 Cordys 作废动作、原因校验和独立权限语义                                                                                                                                                 | `ContractStatus` 缺少明确 `VOID` 阶段；`Contract` 缺少 `voidReason`、`voidedAt/voidedById` 等审计字段；若继续采用可配置阶段，还缺合同阶段配置表                                                   | 先按合同页面与 API 全链路对齐阶段模型，再接消息事件             | `DISCOVERED`  |
| DB-002 | 合同归档 `CONTRACT_ARCHIVED`     | `ContractStage.ARCHIVED`、`ContractService.updateStage`                                                                           | 当前没有归档/完结动作及状态流转                                                                                                                                                                                         | `ContractStatus` 缺少 `ARCHIVED`；`Contract` 缺少 `archivedAt/archivedById`；动态合同阶段配置尚未建模                                                                                             | 与 DB-001 一并完成合同阶段设计                                  | `DISCOVERED`  |
| DB-003 | 发票审批 `INVOICE_APPROVAL`      | `ApprovalActionService.sendFinishNotice` 的 `FormKey.INVOICE` 分支、`DictModule.INVOICE_APPROVAL`                                 | 当前 `InvoiceRecord` 只有待开票/已开票/作废，没有提交审批入口；审批引擎把回款记录作为独立对象，不能冒充发票                                                                                                             | `InvoiceRecord` 缺少 `approvalStatus`、`deptId`、审批关联信息；`ApprovalModule`/审批流缺少 `invoice` 模块                                                                                         | 先完成发票申请页面、审批状态和流程接入                          | `DISCOVERED`  |
| DB-004 | 配置范围中的创建人 `CREATE_USER` | `CommonNoticeSendService.getNoticeReceiveUserIds`、Cordys `BaseModel.createUser`                                                  | W2.4 解析器可识别占位符，但部分业务对象无法提供创建人上下文                                                                                                                                                             | `Quote`、`Contract`、`ReceivablePlan` 等缺少 `createdById`；相关创建接口未写入创建人                                                                                                              | 在对应模块页面/API对齐时统一补审计字段并迁移历史数据            | `DISCOVERED`  |
| DB-005 | 回款计划独立负责人               | `ContractPaymentPlan.owner`、`NoticeExpireJob.buildPaymentPlanNoticeContext`                                                      | 当前回款计划到期通知只能暂用合同负责人                                                                                                                                                                                  | `ReceivablePlan` 缺少 `ownerId`、`deptId`、`createdById`                                                                                                                                          | 对齐回款计划新增/编辑页面和负责人字段                           | `DISCOVERED`  |
| DB-006 | 企业微信消息渠道                 | `MessageTask.weComEnable`、消息设置企微列、`WeComNoticeSender`                                                                    | 已完成逐事件/批量开关、渠道 gate、文本应用消息、缺映射审计、有限重试、超时恢复、分页记录和手工重试                                                                                                                      | 已新增 `weComEnabled` 与 `message_deliveries`；保存外部主体快照、状态、尝试次数、下次时间、provider 消息 ID 和安全错误摘要                                                                        | W3.1 配置、W3.2 映射与 W3.3 迁移/API/页面/自动化全部通过        | `VERIFIED`    |
| DB-007 | 公告                             | `views/system/message/index.vue` 公告 Tab 及公告后端模块                                                                          | 当前消息设置只实现消息通知 Tab                                                                                                                                                                                          | 缺少公告、公告接收范围、已读状态及附件关系模型                                                                                                                                                    | 独立读取公告页面、API、Service、Domain、Mapper 后立项           | `DISCOVERED`  |
| DB-008 | 消息模板与多语言资源             | `MessageTemplateUtils`、后端 i18n 资源                                                                                            | 当前通知正文由业务 Service 中文字符串生成，尚未完全复刻 Cordys 模板变量和多语言渲染                                                                                                                                     | 若沿用资源文件无需业务表；如需租户自定义模板，必须先确认 Cordys 是否存在对应能力，禁止臆造模板表                                                                                                  | 完成国际化公共底座源码对齐                                      | `DISCOVERED`  |
| DB-009 | 审批流版本与基础图结构           | `ApprovalFlowVersion`、`ApprovalNode`、`ApprovalNodeApprover`、`ApprovalNodeLink`                                                 | W2.5 已完成主记录、不可变版本、起止/审批节点、线性连接、实例版本引用及旧线性配置迁移                                                                                                                                    | 已补 `currentVersionId/number/formType/execute flags/deleted/audit`、版本、审批节点扩展和连接；条件节点结构归 DB-012                                                                              | 源码、迁移、API、页面、权限、规则测试、Smoke 和浏览器验收均通过 | `VERIFIED`    |
| DB-010 | 编辑/删除审批的变更暂存与回放    | `HitApprovalAspect`、`ApprovalResourceService`、`ApprovalResourceSnapshot`、`approval_instance.execute_time/update_fields`        | 当前审批只在业务主动提交时创建实例，不能拦截编辑/删除，也没有审批通过后的变更回放和删除执行                                                                                                                             | 审批实例缺 `executeTime/comment/updateFields`；缺资源快照/暂存模型及业务处理器映射                                                                                                                | 先完成 W2.5 版本化结构，再逐模块接入更新/删除触发               | `DISCOVERED`  |
| DB-011 | 高级审批任务、记录与附件         | `ApprovalAddSignTask`、`ApprovalReturnBackRecord`、`ApprovalRecord`、`ApprovalInstanceAttachment`、任务 `type/action/nodeRound`   | 当前任务只支持普通通过/驳回，缺加签、退回、撤回记录、抄送任务类型、节点轮次与附件                                                                                                                                       | 缺加签关系、退回记录、独立审批记录和实例附件表；`ApprovalTask` 缺 `nodeRound/type/action`                                                                                                         | 完成高级审批动作与审批中心页面源码对齐后实施                    | `DISCOVERED`  |
| DB-012 | 高级审批节点配置                 | `ApprovalNodeApprover`、`ApprovalNodeCondition` 及流程设计器配置                                                                  | 基础抄送已在 W3.4.1 落地：节点 `ccUserIds`、`approval_task.taskType=CC`、`my-copied` 查询和首页入口均已闭环；仍缺自动通过/拒绝、连续上级/部门负责人、空审批人兜底、同提交人策略、条件分支、后置动作、字段权限和 Webhook | 已具备基础 cc 用户列表与 CC 任务类型；仍缺动态 approver/fallback/sameSubmitter、条件 JSON、通过/驳回配置、字段权限及 Webhook 配置/执行审计                                                        | 基础 CC 已完成；其余高级节点能力按流程设置后续独立执行单元接入  | `DISCOVERED`  |
| DB-013 | 企微组织同步与资源映射           | `ThirdDepartmentService`、`WeComDepartmentService`、`UserSyncController`、`DataHandleUtils`、组织同步 DTO/Domain/Mapper           | 已完成配置门槛、部门/成员全量快照、差异预览、冲突绑定/跳过、原子应用、幂等、缺失成员禁用、页面、日志和通知                                                                                                              | 已落地部门/成员映射、同步批次/差异、凭据版本、结果与最近同步状态；增量游标、多部门关系、unionId/openUserId、定时同步及其他 provider 另行跟踪                                                      | W3.1 配置底座；W3.2 规格、迁移、API、页面与自动化验收全部完成   | `VERIFIED`    |
| DB-014 | 企微统一登录与外部身份           | `SSOController/Service`、`OAuthStateFlow.QR_WECOM/WECOM`、`sys_user/sys_user_extend` 及组织成员绑定                               | 已完成 PC 官方扫码与工作台 WebView OAuth 双流程、一次性回调、JWT、身份管理、资料补全和分类型登录审计                                                                                                                    | flow 使用 `QR_WECOM/WECOM`；邮箱可空、性别为 Boolean、头像进入独立 `user_extensions`；原始安全材料不落库，全部迁移已从零验证                                                                      | 数据库重建、Seed、生产构建、19 条专项 Smoke 与最终检查均通过    | `VERIFIED`    |
| DB-015 | 钉钉/飞书登录与消息 provider     | `MessageTask.dingTalkEnable/larkEnable`、对应 OAuth、同步与 NoticeSender                                                          | W3.3 仅实现企业微信；钉钉/飞书的配置、成员映射、统一登录和消息 sender 尚未实现                                                                                                                                          | 现有 provider/channel 枚举已预留；仍缺对应安全配置、外部资源映射、登录协议适配与 sender                                                                                                           | 分别读取 Cordys 对应前后端全链路后独立立项                      | `DISCOVERED`  |
| DB-016 | 线索直接模型与线索池分域表       | `Clue`、`ClueOwner`、`ClueField/Blob`、`CluePool`、`CluePoolHiddenField`、Pick/Recycle Rule、`ClueCapacity`                       | 直接模型、分域 Repository、规则、Owner 生命周期、业务调用方、Seed、普通线索/转换/多池 API、分域配置、模块设置 Pool/Capacity/Move Reason 与 PC 页面均已完成并通过 W3.4.2 最终验收                                        | 线索 9 表、字段值事务、池事务、Owner History、Pool/Capacity 配置、Hidden Field、PICK/ASSIGN、自动回收、User View 及 `CLUE_POOL_RS` 原因集成均有真实库/API/Browser 证据                            | W3.4.0 task 1.1～1.9 与 W3.4.2 task 3.1～3.7 全部完成           | `VERIFIED`    |
| DB-017 | 客户/联系人/公海直接模型         | `Customer`、`CustomerOwner`、`CustomerField/Blob`、`CustomerContact*`、`CustomerPool*`、`CustomerCapacity/Relation/Collaboration` | 直接模型、分域 Repository、Seed 与 1.9 关键链路已完成；4.1 已锁定普通客户、联系人、公海、360、协作、关系、合并、Owner History、User View 与 Pool Rule 源码矩阵，API/Page 尚待 4.2～4.7                                  | 现有直接表覆盖 Cordys 最终 DDL；联系人 `customerId` 最终可空，公海进入时间由 `Customer.updateTime` 表示，无需新增列；仍需 `/account`、`/account/contact`、`/pool/account` 分域 API 与四类访问边界 | W3.4.0 task 1.1～1.9、W3.4.3 task 4.1 已完成；继续 4.2～4.7     | `IN_PROGRESS` |
| DB-018 | 仪表板目录、资源与收藏           | `Dashboard`、`DashboardModule`、`DashboardCollection`、`DashboardController/ModuleController`                                     | 三张直接表、Seed 与 1.9 数据审计均已验证；当前 `/reports` 固定 ECharts 页面和旧 Dashboard 统计 API 尚未替换                                                                                                             | `dashboard`、`dashboard_module`、`dashboard_collection` 已进入迁移并通过最终 Seed 审计；第三方嵌入配置边界仍需 W3.4.4 实施                                                                        | W3.4.0 task 1.1～1.9 已完成；继续 W3.4.4 的 5.1～5.6            | `IN_PROGRESS` |
| DB-019 | 用户视图直接模型                 | `UserView`、`UserViewCondition`、`sys_user_view`、`sys_user_view_condition`                                                       | 五类资源 API、Web 契约和业务列表筛选均已切换到 UserViews Service；系统视图保持代码事实，不入用户视图表；1.9 根回归已再次验证 UserView 新建、筛选、固定与删除                                                            | `sys_user_view/sys_user_view_condition` 直接模型、条件文本/树节点序列化、组织/用户/resourceType 隔离均已通过空库迁移、规则测试和真实库 Smoke                                                      | W3.4.0 task 1.1～1.9 已完成                                     | `VERIFIED`    |
| DB-020 | 模块表单、字段与分域字段值       | `ModuleForm/Blob`、`ModuleField/Blob`、`BaseResourceField`、Clue/Customer/Contact Field 与 Blob 表                                | ModuleForms、Metadata adapter 与三目标业务调用方已接入直接字段值；线索页面已验收，4.1 再次确认客户/联系人唯一规则必须覆盖 CRUD、导入、批改和合并，客户/联系人页面验收未完成                                             | 模块表单/字段 4 表及三业务域 Field/Blob 已进入迁移，旧 FieldDefinition 已删除；无新增数据库缺口，图外模块继续由 DB-021 跟踪                                                                       | W3.4.0 task 1.1～1.9 与线索域已完成；继续客户域 4.2～4.7        | `IN_PROGRESS` |
| DB-021 | 图外业务模块分域动态字段值       | Cordys Product/Opportunity/Quotation/Contract/Order/FollowUpPlan 对应 ModuleForm、Field/Blob Domain 与 Mapper                     | 商机、产品、报价、合同、订单和跟进计划仍使用 `customData JSONB`；旧 `FieldDefinition` 已在 W3.4.0 删除，当前 Metadata 由 ModuleForm adapter 维持现有页面可运行，但这些业务域尚未迁移到 Cordys 独立 Field/Blob 表        | 缺各业务域 Cordys `*_field/*_field_blob` 直接表及对应值服务；不得重新引入 FieldDefinition 或以 JSONB 长期冒充 Cordys 直接字段模型                                                                 | 各模块逐页源码对齐时建立直接字段值表                            | `DISCOVERED`  |
| DB-022 | 模块配置专属设置入口             | `views/system/module/components/configCard.vue` 及各模块设置抽屉/页面                                                             | 线索三个设置入口已完成；4.1 已确认客户卡片必须接 `/account-pool/*`、`/account-capacity/*` 与 `/dict/* (CUSTOMER_POOL_RS)`，当前仍为占位；其余商机、回款、发票、合同、订单、价格表入口继续暂缓                           | 客户 Pool/Capacity 直接模型已有，`sys_dict/sys_dict_config` 也已支持 `CUSTOMER_POOL_RS`，预计无需新表；需在 4.5/4.6 接真实 Controller/Drawer。其它模块仍关联 DB-001～DB-005、DB-018、DB-021       | 客户三个入口由 W3.4.3 task 4.5/4.6 完成，其余随各模块逐页实施   | `DISCOVERED`  |
