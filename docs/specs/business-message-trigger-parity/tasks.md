# W2.4 实施任务

- [x] 1. 完成 Cordys 业务消息全链路源码反查
  - 读取客户、联系人、协作人、线索、资源池、商机、报价、审批、到期任务和公共发送服务。
  - 建立 29 个可落地事件与 3 个后置事件的边界。
  - _Requirements: R1-R7_

- [x] 2. 固化需求与技术设计
  - 明确事件矩阵、接收人、排除操作者、到期时间、配置范围、错误隔离和非目标。
  - 建立暂缓能力与数据库缺口台账，登记三个后置事件及已发现的审计/负责人/渠道模型差异。
  - _Requirements: R1-R7_

- [x] 3. 实现统一业务消息分发层
  - 新增 `BusinessNotificationsService`，实现接收人去重、操作者排除、租户/状态过滤和 best-effort 错误隔离。
  - 保持 `NotificationsService` 的事件开关、落库、SSE 和无事件通知兼容语义。
  - 增加公共层单元测试。
  - _Requirements: R1, R7_

- [x] 4. 实现可配置接收范围解析
  - 在消息设置服务中合并默认配置与租户覆盖。
  - 支持 OWNER、CREATE_USER、指定成员、角色成员、当前及上级部门负责人。
  - 对无效、禁用、跨租户用户和空范围进行安全处理，并增加层级解析测试。
  - _Requirements: R1, R6, R7_

- [x] 5. 接入客户、联系人和协作团队事件
  - 接入新增、联系人、协作人、转移、人工/自动移入公海、公海分配和删除 8 个事件。
  - 修正协作事件当前错误发送给协作成员的差异。
  - 统一普通负责人变更与公海分配的事件选择，覆盖单条和批量路径。
  - _Requirements: R2, R7_

- [x] 6. 接入线索与线索池事件
  - 接入新增、转移、人工/自动移池、池内分配、删除、转客户、转商机 8 个事件。
  - 将转客户和转商机拆成两条独立通知，避免合并事件。
  - 统一创建、更新、分配和池内分配的事件选择，覆盖单条和批量路径。
  - _Requirements: R3, R7_

- [x] 7. 接入商机、报价和审批结果事件
  - 接入商机新增、转移、删除以及报价删除事件。
  - 为报价、合同、订单审批完成/驳回映射准确事件。
  - 保持审批待办和回款审批为无目录事件的兼容通知，不冒充发票审批。
  - _Requirements: R4, R7_

- [x] 8. 实现统一到期通知执行器
  - 新增每天 08:00 执行的 `MessageExpiryService`，替换固定 3 天回款提醒任务。
  - 接入报价、合同、回款计划的即将到期和到期 6 个事件。
  - 按租户读取开关、`timeList` 和接收范围，使用精确日历日窗口和业务状态过滤。
  - 使用固定时钟测试覆盖 3/7 天、当天到期、足额回款、无配置和关闭事件。
  - _Requirements: R5, R6, R7_

- [x] 9. 扩展集成测试与 Smoke
  - 验证事件关闭不落库、恢复开关后重新发送。
  - Smoke 覆盖客户人工移池、线索带商机转换、商机转移和审批结果；固定时钟测试覆盖可配置到期提醒。
  - 测试结束恢复全部消息开关和默认 3 天配置。
  - _Requirements: R1-R7_

- [x] 10. 完成全量验证、文档同步和本地提交
  - 运行 Prisma 校验、shared/API/Web typecheck、ESLint、API/Web build、规则测试和全链路 Smoke。
  - 更新 Wave 2 执行计划、parity、alignment log、API/架构/文档索引和断言计数。
  - 浏览器复验消息设置及受影响业务页面后创建本地 Git 提交。
  - 提交前复查暂缓台账；本阶段未解决的数据库缺口必须保留明确状态和前置条件。
  - _Requirements: R7_
