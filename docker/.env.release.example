# 镜像版本：默认使用 latest；也可指定 v0.0.8 这类 Git tag。
APP_VERSION=latest

# 可选：fork 或私有镜像仓库时取消注释并覆盖。
# API_IMAGE=ghcr.io/hideinmatrix/micromatrix-crm-api:latest
# MIGRATE_IMAGE=ghcr.io/hideinmatrix/micromatrix-crm-migrate:latest
# WEB_IMAGE=ghcr.io/hideinmatrix/micromatrix-crm-web:latest

# PostgreSQL。默认 release compose 内置 postgres 服务；外部数据库可直接替换该 URL。
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_me_postgres_password
POSTGRES_DB=default
DATABASE_URL=postgresql://postgres:change_me_postgres_password@postgres:5432/default?schema=public

# Redis。仅供 Compose 内部 API 使用，不对宿主机开放 6379。
REDIS_PASSWORD=change_me_redis_password
REDIS_DB=0

# API 生产密钥，部署前必须替换。
JWT_ACCESS_SECRET=change_me_with_a_strong_random_access_secret
JWT_REFRESH_SECRET=change_me_with_a_strong_random_refresh_secret
INTEGRATION_CREDENTIALS_KEY=change_me_with_at_least_32_random_characters

# 对外 Web 地址及企微默认租户。
WEB_PUBLIC_URL=http://localhost:8080
WECOM_DEFAULT_TENANT_SLUG=demo

# API 位于单层 web/Nginx 反向代理之后；本地直接运行 API 时不要设置该值。
TRUST_PROXY_HOPS=1

# 操作日志保留与有界清理。默认每天仅清理最多 20 * 1000 条过期记录。
OPERATION_LOG_RETENTION_DAYS=180
OPERATION_LOG_CLEANUP_BATCH_SIZE=1000
OPERATION_LOG_CLEANUP_MAX_BATCHES=20

# Docker stdout/stderr 日志轮转；长期服务默认每容器保留最多 5 个 20 MiB 文件。
DOCKER_LOG_MAX_SIZE=20m
DOCKER_LOG_MAX_FILE=5
